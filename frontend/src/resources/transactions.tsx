//Source code generated by AppGPT (www.appgpt.tech)
import {
  Datagrid,
  List,
  EditButton,
  Edit,
  SimpleForm,
  Create,
  SelectColumnsButton,
  DatagridConfigurable,
  TopToolbar,
  CreateButton,
  ExportButton,
  FilterButton,
  //Field controls
  BooleanField,
  DateField,
  EmailField,
  ImageField,
  NumberField,
  ReferenceField,
  TextField,
  UrlField,
  //Input controls
  BooleanInput,
  DateInput,
  EmailInput,
  ImageInput,
  NumberInput,
  ReferenceInput,
  TextInput,
  UrlInput,
  PasswordInput,
} from 'react-admin';
import { useRecordContext } from 'react-admin';
import { Grid } from '@mui/material';
const ReadOnlyPasswordField = ({ record, source }) => {
  // You can customize the way you display the password here, e.g., mask it with asterisks
  const maskedPassword = '********';

  return <span>{maskedPassword}</span>;
};
const ListActions = () => (
  <TopToolbar>
    <FilterButton />
    <CreateButton />
    <ExportButton />
    <SelectColumnsButton />
  </TopToolbar>
);
const transactionsTitle = () => {
  const record = useRecordContext();
  return <span>transactions {record ? `"${record.medicationId}"` : ''}</span>;
};

export const transactionsList = () => (
  <List actions={<ListActions />} filters={ResourceFilters}>
    <DatagridConfigurable>
      <ReferenceField source="medicationId" reference="medications" />
      <NumberField source="quantityDispensed" />
      <NumberField source="remainingQuantity" />
      <NumberField source="totalCost" />
      <EditButton />
    </DatagridConfigurable>
  </List>
);

export const transactionsEdit = () => (
  <Edit title={<transactionsTitle />}>
    <SimpleForm>
      <Grid container rowSpacing={1} columnSpacing={{ xs: 1, sm: 2, md: 5 }}>
        <Grid item xs={4}>
          <ReferenceInput source="medicationId" reference="medications" />
        </Grid>
        <Grid item xs={4}>
          <NumberInput source="quantityDispensed" />
        </Grid>
        <Grid item xs={4}>
          <NumberInput source="remainingQuantity" />
        </Grid>
        <Grid item xs={4}>
          <NumberInput source="totalCost" />
        </Grid>
      </Grid>
    </SimpleForm>
  </Edit>
);

export const transactionsCreate = () => (
  <Create>
    <SimpleForm>
      <Grid container rowSpacing={1} columnSpacing={{ xs: 1, sm: 2, md: 5 }}>
        <Grid item xs={4}>
          <ReferenceInput source="medicationId" reference="medications" />
        </Grid>
        <Grid item xs={4}>
          <NumberInput source="quantityDispensed" />
        </Grid>
        <Grid item xs={4}>
          <NumberInput source="remainingQuantity" />
        </Grid>
        <Grid item xs={4}>
          <NumberInput source="totalCost" />
        </Grid>
      </Grid>
    </SimpleForm>
  </Create>
);

const ResourceFilters = [
  <TextInput source="q" label="Search" alwaysOn />,
  <ReferenceInput
    source="medicationId"
    label="medicationId"
    reference="medications"
    alwaysOn
  />,
  ,
  ,
  ,
];
